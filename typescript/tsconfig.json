{
  // 1. 상속 (Inheritance)
  // 부모 폴더(..)에 있는 공통 tsconfig.json 설정을 가져옵니다.
  // 이를 통해 strict 모드, module 시스템 등 핵심 규칙을 모든 서브 프로젝트에 일관되게 적용합니다.
  "extends": "../tsconfig.json",

  "compilerOptions": {
    // 2. 소스 코드의 뿌리 (Root Directory)
    // 컴파일 대상이 되는 소스 파일들이 모여 있는 "진짜" 시작 폴더를 지정합니다.
    // 이 설정이 있으면 빌드 시 src 폴더 바깥의 파일이 포함되는 것을 방지하고,
    // 빌드 결과물(dist)에서 src 폴더 내부의 구조가 그대로 유지됩니다.
    "rootDir": "./src",

    // 3. 결과물 출력 경로 (Output Directory)
    // TypeScript 파일(.ts)이 JavaScript(.js)로 변환되어 저장될 폴더입니다.
    // 소스 코드(src)와 실행 코드(dist)를 분리하여 프로젝트를 깔끔하게 유지합니다.
    "outDir": "./dist"
  },

  // 4. 포함 범위 (Inclusion)
  // TypeScript가 "어떤 파일을 검사하고 변환할지" 결정하는 패턴입니다.
  // "src/**/*"의 의미:
  // - src/: src 폴더 안에서
  // - **/: 모든 하위 폴더를 뒤져서
  // - *: 모든 파일명을 대상으로 함
  "include": ["src/**/*"],

  // 5. 제외 범위 (Exclusion)
  // 컴파일 대상에서 명시적으로 제외할 항목들입니다.
  // - node_modules: 외부 라이브러리 폴더는 검사할 필요가 없음
  // - dist: 이미 컴파일된 결과물이므로 다시 검사할 필요가 없음 (무한 루프 방지)
  "exclude": ["node_modules", "dist"]
}
